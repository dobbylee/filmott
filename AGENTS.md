# filmott 작업 규칙

이 파일은 Codex가 따르는 프로젝트 작업 규칙이다. Claude 전용 설정과 sub-agent 정의는 `CLAUDE.md` 및 `.claude/` 아래에서 별도로 관리한다.

## 기본 작업 방식

- 기존 코드 구조와 로컬 패턴을 먼저 확인하고, 변경 범위는 요청 해결에 필요한 만큼만 유지한다.
- 구현 전에 확인 가능한 성공 조건과 변경하지 않을 범위를 정한다.
- 현재 요청에 필요하지 않은 미래 확장용 추상화, 설정화, 리팩터링을 추가하지 않는다.
- 사용자가 구현을 요청하면 계획 제안에서 멈추지 않고 구현, 검증, 결과 보고까지 진행한다.
- 사용자가 명시적으로 요청하지 않은 리팩터링, 포맷 변경, 메타데이터 변경은 피한다.
- 사용자가 만든 변경은 되돌리지 않는다. 같은 파일에 섞여 있으면 읽고 맞춰서 작업한다.

## Codex 계획/구현 흐름

- Codex는 기본적으로 sub-agent 없이 계획과 구현을 수행하되, 중요한 구현 변경은 아래 커밋 단위 리뷰 흐름에 따라 독립 reviewer 검증을 받는다.
- 기능 추가, 구조 변경, 다중 파일 수정, 리스크가 있는 버그 수정은 구현 전 계획 문서를 작성할지 사용자에게 먼저 확인한다.
- 사용자가 계획 문서 작성을 승인하면 `docs/plans/YYYY-MM-DD_제목.md` 형식으로 작성한다.
- 계획 문서 작성 후에는 사용자가 문서를 확인할 수 있도록 경로와 요약을 전달하고 작업을 멈춘다.
- 사용자가 계획 문서 수정 요청을 하면 문서를 수정하고 다시 확인을 요청한다.
- 사용자가 작업 진행을 요청하면 승인된 계획 문서를 기준으로 다음 동작을 진행한다.
- 단순 오타, 작은 문구 수정, 명확한 한 줄 수정처럼 계획 문서 비용이 더 큰 작업은 계획 문서를 생략할 수 있다.
- 계획 문서는 기존 파일을 덮어쓰지 않고 새 파일로 생성한다.
- 계획 문서에는 목표, 현재 구조 요약, Phase 1/2/... 구현 단위, 각 Phase의 변경 파일, 검증 방법, 리스크를 적는다.
- 각 Phase는 가능하면 독립적으로 구현하고 커밋할 수 있는 단위로 나눈다.
- 구현은 승인된 계획 문서를 기준으로 진행하며, 계획 밖 변경이 필요해지면 먼저 계획 문서를 업데이트하고 이유를 남긴다.
- 구현 중에는 Phase 하나를 완료할 때마다 해당 범위의 테스트와 변경 앱의 `npm run lint`, `npm test`, `npm run build`를 확인한다.
- 백엔드 변경은 Phase 검증에 `npm run test:e2e`를 포함한다.
- DB 스키마, 마이그레이션, TypeORM relation, raw SQL, 리뷰/감상기록, 검색, 랭킹 변경은 `npm run test:integration`을 추가로 확인한다.

## 커밋 단위 구현/리뷰 흐름

- 상세 절차와 하네스 개선 기준은 `agent-harness/workflow.md`를 따른다.
- 작업은 하나의 목적과 검증 기준으로 설명할 수 있는 가장 작은 커밋 단위로 나눈다.
- 프로덕션 코드의 기능 추가, 버그 수정, 구조 변경, 공통 계약 변경은 커밋 전에 프로젝트 `reviewer` sub-agent의 독립 리뷰를 받는다.
- 구현과 관련 검증 후 reviewer가 변경 파일만 검토하게 하고, 지적 사항을 수정한 뒤 같은 범위를 다시 검토한다.
- reviewer가 정확히 `No Findings`를 반환할 때까지 리뷰-수정-재검증을 반복한다.
- `No Findings` 이후 검증 실패로 코드가 바뀌면 변경 범위를 다시 리뷰한다.
- 단순 문구/오타, 일시적이고 되돌릴 실험, 사용자가 명시적으로 리뷰 생략을 요청한 작은 변경은 영향도가 낮을 때 리뷰를 생략할 수 있다.
- 계획 문서 기반 작업은 이 반복을 Phase별로 완료한 뒤 해당 Phase만 커밋한다.
- 최종 보고에는 실행한 검증, reviewer 결과, 생략한 검증과 이유를 포함한다.

## 하네스 개선

- 버그, 리뷰 발견, 수동 검증 누락이 발생해도 개별 사례를 곧바로 영구 규칙으로 만들지 않는다.
- 심각도, 재발 가능성, 일반 작업에서의 탐지 난이도, 작고 실행 가능한 검증 여부를 `agent-harness/workflow.md` 기준으로 평가한다.
- 기준을 충족한 문제는 테스트, CI, reviewer prompt, workflow, `AGENTS.md` 중 가장 작고 실행 가능한 계층에 반영한다.
- 새 규칙에는 적용 조건, 요구 행동, 검증 지점이 있어야 한다. 기존 규칙으로 충분하면 새 항목을 추가하지 않고 기존 규칙이나 검증을 보강한다.
- 특정 기능, 외부 서비스, 모델의 일회성 사실은 저장소 전체 하네스 규칙으로 일반화하지 않는다.

## 검증

- 변경한 앱(`frontend`, `backend`)마다 커밋 전 `npm run lint`, `npm test`, `npm run build`를 통과시킨다.
- 백엔드 변경은 커밋 전 `npm run test:e2e`도 통과시킨다.
- DB 스키마, 마이그레이션, TypeORM relation, raw SQL, 리뷰/감상기록, 검색, 랭킹 변경은 `npm run test:integration`도 통과시킨다.
- 프론트 API client, 인증, 리뷰 작성/수정 흐름 변경은 `npm test`에 포함된 MSW 기반 integration 결과를 함께 확인한다.
- Playwright e2e는 주요 사용자 여정 변경 또는 릴리즈 전 smoke 검증으로 실행한다.
- `lint`는 검사 전용으로 유지하고, 자동 수정이 필요할 때만 `lint:fix`를 사용한다.
- 테스트 실행은 `npm test`를 사용하고 `npx jest`, `npx vitest` 직접 호출은 피한다.
- 테스트 설명은 한국어로 작성한다.
- 검증을 실행하지 못했거나 일부만 실행했다면 최종 보고에 명확히 남긴다.

## 타입/린트

- 프로덕션 코드에서 `any`를 추가하지 않는다.
- 외부 입력은 `unknown`으로 받고 타입 좁히기 후 사용한다.
- `JSON.parse`, 외부 API 응답, cookie/request 값, TypeORM raw query 결과는 바로 신뢰하지 않는다.
- `getRawOne`, `getRawMany`, `queryRunner.query`는 반환 타입을 명시한다.
- lint 문제는 규칙 비활성화보다 코드 구조 수정으로 해결한다.
- lint 예외는 테스트 범위(`*.spec.ts`, `test/**`)로만 제한한다.

## Git 규칙

- 이미 push된 커밋에 `--amend`를 사용하지 않는다. 항상 새 커밋으로 수정한다.
- 계획 문서 기반 작업은 Phase별 검증 통과 후 Phase 단위로 커밋한다.
- 커밋 제목에는 변경 성격에 맞는 prefix를 붙인다. 예: `feat:`, `fix:`, `refactor:`, `test:`, `docs:`, `chore:`
- 커밋 제목과 본문은 한국어로 작성한다. prefix는 영어 conventional commit 형식을 유지하고, 설명 문장은 한국어로 쓴다.
- 커밋은 제목만 작성하지 않고, 본문에 주요 변경 내용과 검증/영향 범위를 1~3문단으로 요약한다.
- 서로 성격이 다른 변경은 별도 커밋으로 분리한다. 예: 설정 문서 변경과 기능 구현 변경은 따로 커밋한다.
- `.gitignore` 대상 파일은 사용자가 명시적으로 문서/ignored 파일 커밋을 요청한 경우에만 `git add -f`로 포함한다. 문서 수정이 필요하더라도 기본적으로 로컬 워킹트리에만 남기고 기능 커밋에는 섞지 않는다.
- UI 확인이 필요한 Phase는 로컬 검증 결과와 확인 방법을 보고하고, 사용자가 확인해야 할 범위를 명확히 남긴다.
- 계획 문서 없이 처리한 작은 변경은 사용자가 커밋을 명시적으로 요청했을 때만 커밋한다.
- `git push`, `git reset --hard`, `rm -rf` 같은 파괴적/원격 반영 명령은 사용자의 명시 요청 없이 실행하지 않는다.

## 서버 규칙

- 테스트용 서버를 백그라운드로 실행하지 않는다. 서버는 사용자가 직접 관리한다.
- 사용자가 직접 확인해야 하는 UI 작업은 검증 방법과 URL 또는 경로를 명확히 안내한다.

## DB 규칙

- `synchronize: false`를 유지한다. entity 변경 시 ALTER TABLE 수동 실행 또는 마이그레이션 추가가 필요하다.
- Pagination 마지막 페이지 버튼은 의도적으로 미표시한다. API 10000건 제한을 고려한 정책이다.
- DB 변경은 프로덕션 백업 배치가 운영 중인 전제를 두되, 스키마/데이터 변환/롤백 영향은 더 보수적으로 확인한다.
- 마이그레이션은 가능한 범위에서 TypeORM `QueryRunner`의 스키마 조작 메서드와 `TableColumn` 등 구조화 API를 우선 사용한다. 데이터 변환, `USING`, DB 함수, 복잡한 backfill처럼 구조화 API로 정확히 표현하기 어려운 경우에만 raw SQL을 최소 범위로 사용하고 이유를 남긴다.
- `TRUNCATE CASCADE`, `DROP CASCADE` 등 연쇄 삭제가 발생하는 명령은 실행 전 영향 받는 테이블 목록을 보여주고 사용자 승인 후 실행한다.
- 백업 dump 파일은 복원 완료 확인 전까지 삭제하지 않는다.

## 배포 규칙

- push 전 로컬 빌드/동작 검증을 완료한다. 추측으로 답하지 않고 검증 결과를 보여준다.
- Docker 이미지 변경은 `docker build --platform linux/arm64` 로컬 빌드 성공 확인 후 push한다.
- 마이그레이션은 `npm run migration:run` 로컬 실행 확인 후 push한다.
- 컨테이너 재시작 후 nginx 재시작과 외부 접속 확인을 수행한다.
- 외부 접속 확인은 `curl -s -o /dev/null -w '%{http_code}' https://filmott.kr` 결과 200을 기준으로 한다.

## UI/UX 규칙

- 비용이 크지 않은 수준이라면 UX를 우선한다. 소규모 API 비용으로 사용자 경험이 개선된다면 적극 선택한다.
- 주요 버튼은 `bg-primary`보다 `from-fuchsia-700 to-indigo-600` 그라데이션을 우선 사용한다.

## 기타

- `.env*` 파일은 사용자의 명시 요청 없이 수정하지 않는다.
