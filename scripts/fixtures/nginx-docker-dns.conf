map $host $filmott_active_slot { default "blue"; }

server {
    listen 8080;
    resolver 127.0.0.11 valid=1s ipv6=off;
    resolver_timeout 1s;

    location / {
        set $filmott_active_backend backend-$filmott_active_slot:80;
        proxy_pass http://$filmott_active_backend;
    }
}
